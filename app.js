// app.js - Avini Labs core logic

console.log("Welcome to Avini Labs software");

// Example function for patient registration
function registerPatient(name, age, gender) {
  console.log(`Patient Registered: ${name}, Age: ${age}, Gender: ${gender}`);
}

// Example function for billing
function generateBill(patient, amount) {
  console.log(`Bill generated for ${patient}: ₹${amount}`);
}

// Example function for test report entry
function addTestReport(patient, test, result) {
  console.log(`Report added: ${patient}, Test: ${test}, Result: ${result}`);
}
